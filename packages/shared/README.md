# `@verdin/shared`

Shared TypeScript types and constants used across `apps/web` and other frontend packages.

```bash
pnpm --filter @verdin/shared build
```

Source: `src/`. Prefer importing from this package instead of duplicating domain enums/types in the web app.
