[project]
name = "verdin-document-classification"
version = "0.1.0"
description = "Shared rule-based document classification engine for Verdin API and worker"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["verdin_document_classification"]
