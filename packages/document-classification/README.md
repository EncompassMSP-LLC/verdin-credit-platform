# Document classification

Python package used by the API/worker pipeline to classify uploaded documents (credit report vs ID vs other).

Installed into API/worker images from the monorepo `packages/` tree. See worker OCR/import jobs and `apps/api` document modules for call sites.
