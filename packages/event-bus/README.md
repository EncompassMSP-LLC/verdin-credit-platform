# Event bus

Python event-bus helpers for publishing/consuming internal domain events (timeline, jobs, integrations).

Wired through API services and worker jobs. Event payloads live in `packages/event-types`.
