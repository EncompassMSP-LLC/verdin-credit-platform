[project]
name = "verdin-entity-resolution"
version = "0.1.0"
description = "Shared entity resolution engine for Verdin platform"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["verdin_entity_resolution"]
