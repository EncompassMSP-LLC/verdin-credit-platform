# Entity resolution

Python package for matching tradelines / entities across bureau reports (cross-bureau keys, merge helpers).

Used when importing parsed credit reports and when running case entity re-resolve. See Version 27.0 playbook/entity work and `apps/api` accounts/documents modules.
