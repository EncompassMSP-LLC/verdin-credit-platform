# Document metadata

Python helpers for extracting and normalizing document metadata during ingest (OCR/import path).

Consumed by `apps/api` and `apps/worker`. Pair with `document-classification` and `report-parsers` in the document pipeline.
