[project]
name = "verdin-document-metadata"
version = "0.1.0"
description = "Rule-based document metadata extraction for Verdin"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["verdin_document_metadata"]
