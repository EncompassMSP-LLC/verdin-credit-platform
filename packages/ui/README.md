# `@verdin/ui`

Shared React UI primitives (Button, Card, Badge, …) for `apps/web`.

```bash
pnpm --filter @verdin/ui build
```

Source: `src/`. Keep presentation here; page-specific composition stays in `apps/web`.
