[project]
name = "verdin-event-types"
version = "0.1.0"
description = "Typed platform event definitions for Verdin"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["verdin_event_types"]
