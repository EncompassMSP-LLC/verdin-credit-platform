[project]
name = "verdin-report-parsers"
version = "0.1.0"
description = "Pluggable credit bureau report parsers for Verdin"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["verdin_report_parsers"]
