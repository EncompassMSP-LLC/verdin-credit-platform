# `@verdin/validation`

Shared Zod schemas for forms and client-side validation in `apps/web`.

```bash
pnpm --filter @verdin/validation build
```

Source: `src/`. Align field rules with API pydantic schemas where practical; API remains the source of truth for persistence.
