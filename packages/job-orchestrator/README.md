# `packages/job-orchestrator`

Shared Redis job queue helpers used by the API (enqueue) and worker (consume).

See ADR-011 / worker README. Install via editable path from `apps/api/requirements.txt` and `apps/worker/requirements.txt`.
