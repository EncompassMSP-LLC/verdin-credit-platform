---
description: Version 5.0+ sprint loop — sync, slice, verify, PR, auto-merge
alwaysApply: true
---

# Version 5.0+ Sprint Loop

Use this loop when continuing Product Hardening / pilot UI work without waiting for manual "merge and continue" messages.

## When triggered

- User says merge and continue, execute next steps, follow recommended order, or similar
- Cursor Automation fires after a PR merges to `main`
- You detect `main` advanced since the last slice (via `git fetch` + log)

## Loop steps (always in order)

1. **Sync** — `git checkout main && git pull origin main`
2. **Scope** — pick the next small, reviewable Version 5.0+ slice from [`docs/development/version-5.0-plus-completion-checklist.md`](../../docs/development/version-5.0-plus-completion-checklist.md)
3. **Branch** — `git checkout -b feature/<short-name>` from synced `main`
4. **Implement** — backend (if needed) + tests + `@verdin/api-client` + UI + docs
5. **Verify**
   - `python -m pytest` for touched API tests (`DATABASE_URL` → `verdin_credit_test`)
   - `pnpm --filter @verdin/api-client build` before web typecheck
   - `pnpm --filter @verdin/web typecheck` and `lint` when web changes
   - pre-commit hooks must pass before commit
6. **Ship** — conventional commit, push, open PR to `main`
7. **Auto-merge** — after `gh pr create`, run:
   ```bash
   gh pr merge --auto --squash
   ```
8. **Continue** — do not stop for user input unless blocked; after merge, start step 1 again

## Slice sizing

- One UI surface, one DX fix, or one small API gap per PR
- Follow existing patterns in `apps/web/src/pages/` and `packages/api-client/`
- Update `docs/governance/capability-matrix.md` Frontend column when UI ships
- LLM UI must respect ADR-012 gates

## GitHub CLI (Windows)

`C:\Program Files\GitHub CLI\gh.exe`

## Blockers — stop and report

- CI failing after reasonable fix attempts
- Ambiguous product decisions
- Compliance/legal sign-off required before compliance enforcement UI
