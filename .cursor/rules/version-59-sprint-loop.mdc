---
description: Version 5.9 sprint loop — sync, slice, verify, PR, auto-merge
alwaysApply: true
---

# Version 5.9 Sprint Loop

Use this loop when continuing compliance-gated autonomous production work without waiting for manual "merge and continue" messages.

## When triggered

- User says merge and continue, execute next steps, follow recommended order, or similar
- Cursor Automation fires after a PR merges to `main`
- You detect `main` advanced since the last slice (via `git fetch` + log)

## Loop steps (always in order)

1. **Sync** — `git checkout main && git pull origin main`
2. **Scope** — pick the next small, reviewable Version 5.9 slice from [`docs/development/version-5.9-completion-checklist.md`](../../docs/development/version-5.9-completion-checklist.md)
3. **Branch** — `git checkout -b feature/<short-name>` from synced `main`
4. **Implement** — backend + tests + `@verdin/api-client` + UI (if applicable) + docs
5. **Verify**
   - `python -m pytest` for touched API tests (`DATABASE_URL` → `verdin_credit_test`)
   - `pnpm --filter @verdin/api-client build` before web typecheck
   - `pnpm --filter @verdin/web typecheck` and `lint` when web changes
   - pre-commit hooks must pass before commit
6. **Ship** — conventional commit, push, open PR to `main`
7. **Auto-merge** — after `gh pr create`, run:
   ```bash
   gh pr merge --auto --squash
   ```
8. **Continue** — do not stop for user input unless blocked; after merge, start step 1 again

## Slice sizing

- One unsupervised loop audit surface, one bureau filing endpoint group, one live Stripe Tax scaffold, or one SAML rotation run table per PR
- Follow existing patterns in `apps/api/api/modules/llm/`, `apps/api/api/modules/compliance/`, `apps/api/api/modules/billing/`, and `apps/api/api/modules/enterprise/`
- Update `docs/api/reference.md`, `docs/governance/capability-matrix.md`, and checklist when shipping
- Agent and dispute slices require compliance gates; no arbitrary execution or unsupervised re-filing without deferral docs
- Live Stripe Tax API calls and automated charge retries remain explicitly deferred to 5.10+

## GitHub CLI (Windows)

`C:\Program Files\GitHub CLI\gh.exe`

## Blockers — stop and report

- CI failing after reasonable fix attempts
- Migration conflicts or ambiguous product decisions
- Billing/legal/compliance sign-off required before live external bureau or Stripe Tax integration
