# Release Notes — Version 4.3.0 GA (Operational Core)

**Version:** 4.3.0  
**Date:** 2026-06-29  
**Tag:** `v4.3.0`

## Summary

Version 4.3.0 is the **Operational Core** general availability release — a cohesive credit operations platform with case management, account intelligence, document processing, timeline audit, task management, and the **Mission Control** operational dashboard.

## What's included

| Capability                    | Highlights                                                 |
| ----------------------------- | ---------------------------------------------------------- |
| Case Management               | Full CRUD, filters, RBAC, staff UI                         |
| Credit Account Intelligence   | Tradeline scoring, dispute readiness, intelligence summary |
| Document Foundation           | Upload, versioning, MinIO storage, duplicate detection     |
| OCR Pipeline                  | Async worker extraction (pypdf + tesseract)                |
| AI Classification             | Rule-based document type classification                    |
| Metadata Extraction           | Structured field extraction from documents                 |
| Entity Resolution             | Deterministic case/account/person matching                 |
| Timeline & Audit              | Append-only event stream via platform event bus            |
| Task Management               | CRUD, complete/reopen, overdue filters, timeline events    |
| **Mission Control Dashboard** | Single `GET /api/v1/dashboard` — operations product API    |

## Mission Control Dashboard

The dashboard is a **product**, not a page. One API call returns:

- `overview` — live executive strip
- `cases`, `accounts`, `documents`, `tasks` — domain metrics
- `processing` — document pipeline health
- `performance` — business throughput
- `timeline` — recent activity feed
- `alerts` — OCR failures, unmatched entities, review queue, overdue tasks

The staff UI polls on `refresh_seconds` (30s) and is designed for future WebSocket push without contract changes.

## Stabilization (post-GA)

Before starting Version 4.5 automation work:

1. Run end-to-end workflows (case → upload → OCR → classification → entity resolution → task → timeline → dashboard)
2. Improve test coverage where gaps appear
3. Profile API performance and dashboard aggregation queries
4. Review security settings and dependency versions

## What comes next — Version 4.5

Automation and intelligence — not foundational CRUD:

1. Credit Report Import Wizard
2. AI Case Assistant
3. Dispute Generation Engine
4. Workflow automation
5. Client Portal

See [`docs/roadmap/README.md`](../roadmap/README.md) for the full 4.5 phase plan.
